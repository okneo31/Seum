// v0.4a-5b: 계약·자산·받아서 — 원본 프로그래밍예시.md 의 금융 예시.
가져오기(금융).

계약 나의은행 {
    // 은행 금고에는 BTC가 들어갑니다.
    자산 금고: BTC

    // '예금' 함수는 외부에서 BTC를 받아서 '금고'에 누적합니다.
    함수 예금(받은돈: BTC) 받아서 -> 금고 {}
}

나의은행.예금(BTC(50000)).
나의은행.예금(BTC(30000)).
보여주기(나의은행.금고).
